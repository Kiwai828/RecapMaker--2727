{
  "name": "voicerecap-cloudflare-worker",
  "private": true,
  "scripts": {
    "build": "sh scripts/cloudflare-build.sh",
    "deploy": "npm run build && cd cloudflare && npx wrangler deploy && npx wrangler d1 migrations apply voicerecap-db --remote",
    "preview": "npx wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.122.0"
  }
}
