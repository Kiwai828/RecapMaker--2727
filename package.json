{
  "name": "voicerecap-cloudflare-worker",
  "private": true,
  "scripts": {
    "build": "cd cloudflare && uvx --from workers-py pywrangler sync",
    "deploy": "cd cloudflare && uvx --from workers-py pywrangler deploy",
    "preview": "cd cloudflare && uvx --from workers-py pywrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.122.0"
  }
}
