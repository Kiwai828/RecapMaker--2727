{
  "name": "voicerecap-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uv run pywrangler dev",
    "deploy": "uv run pywrangler deploy",
    "d1:migrate:local": "npx wrangler d1 migrations apply voicerecap-db --local",
    "d1:migrate:remote": "npx wrangler d1 migrations apply voicerecap-db --remote",
    "check": "python3 -m py_compile src/*.py"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
